% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-showcase.R
\name{showcase_metric}
\alias{showcase_metric}
\title{Quantitative metric(s) for a preprocessing-step ordering pitfall}
\usage{
showcase_metric(name, seed = 1L, params = NULL)
}
\arguments{
\item{name}{Which pitfall to evaluate (see \code{\link[=sim_pitfall]{sim_pitfall()}})}

\item{seed}{Integer random seed. Defaults to \code{1}}

\item{params}{Optional \code{\link[=sim_params]{sim_params()}} list; \code{NULL} uses scenario-tuned
defaults}
}
\value{
A data frame with columns \code{metric}, \code{wrong}, \code{right}, and \code{better}
(whether a \code{"lower"} or \code{"higher"} value is the desired outcome).
}
\description{
A convenience wrapper that runs \code{\link[=sim_pitfall]{sim_pitfall()}} without plotting and
returns just its metric table --- useful for programmatic checks and tests.
}
\examples{
showcase_metric("naive_downsample", seed = 1)

}
\seealso{
\code{\link[=sim_pitfall]{sim_pitfall()}}
}
