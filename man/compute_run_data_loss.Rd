% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-render_report.R
\name{compute_run_data_loss}
\alias{compute_run_data_loss}
\title{Compute percent data lost for a given run/block}
\usage{
compute_run_data_loss(eyeris, run_num, eye_suffix = NULL)
}
\arguments{
\item{eyeris}{An \code{eyeris} object containing preprocessing results}

\item{run_num}{Run identifier (numeric or character, e.g. \code{1} or \code{"01"})}

\item{eye_suffix}{Optional eye suffix (e.g., "eye-L", "eye-R") used to select
the correct eye from a binocular object}
}
\value{
A numeric percentage in \verb{[0, 100]}, or \code{NA_real_} when it cannot be
determined
}
\description{
Calculates the proportion of samples in the raw pupil timeseries that are
invalid (i.e., missing/during a blink, or off-screen) and expresses it as a
percentage. This surfaces data loss directly in the report to reinforce
workflow transparency.
}
\details{
Prefers the canonical \code{prop_invalid} metric stored in
\code{eyeris$confounds$unepoched_timeseries} (computed by
\code{\link[=summarize_confounds]{summarize_confounds()}}). Falls back to computing the proportion of
missing samples directly from the raw timeseries when confounds are
unavailable.
}
\keyword{internal}
