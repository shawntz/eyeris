# Extract event identifiers from event messages

Extracts identifiers (like image names or trial numbers) from event
messages to enable matching between start and end events.

## Usage

``` r
extract_event_ids(events)
```

## Arguments

- events:

  Data frame containing event messages

## Value

A vector of extracted identifiers
