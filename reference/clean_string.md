# Clean string by removing non-alphanumeric characters

Removes all non-alphanumeric and non-whitespace characters from a
string.

## Usage

``` r
clean_string(str)
```

## Arguments

- str:

  The string to clean

## Value

A cleaned string with only alphanumeric characters and spaces
