% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-regularize.R
\name{regularize}
\alias{regularize}
\title{Regularize an irregularly-sampled pupil timeseries onto a uniform grid}
\usage{
regularize(eyeris, max_inflation = 2, verbose = TRUE, call_info = NULL)
}
\arguments{
\item{eyeris}{An object of class \code{eyeris} derived from \code{\link[=load_asc]{load_asc()}}}

\item{max_inflation}{The maximum factor by which a block's row count may grow
during grid reconstruction. Blocks whose reconstruction would exceed this
factor are left unchanged (with a warning), guarding against fabricating data
for systematically-decimated recordings or unsplit recording pauses. Defaults
to \code{2} (i.e., reconstruction may at most double the number of rows). Set to
\code{Inf} to always reconstruct}

\item{verbose}{A flag to indicate whether to print detailed logging messages.
Defaults to \code{TRUE}}

\item{call_info}{A list of call information and parameters. If not provided,
it will be generated from the function call}
}
\value{
An \code{eyeris} object whose \code{timeseries} blocks have been placed on a
uniform time grid, with a new logical \code{is_resampled} column marking inserted
rows.
}
\description{
Reconstructs a uniform (consistently-spaced) time grid for each recording
segment by inserting placeholder rows wherever the tracker dropped samples.
}
\details{
Most of the \code{eyeris} pipeline (e.g., \code{\link[=detransient]{detransient()}},
\code{\link[=lpfilt]{lpfilt()}}, \code{\link[=downsample]{downsample()}}) assumes a fixed sampling rate.
EyeLink trackers honor that assumption by zero-filling missing pupil samples,
but some hardware instead \emph{drops} samples entirely when pupil data is
missing, leaving holes in the otherwise evenly-spaced time vector. Those
holes silently distort any rate-dependent step.

\code{regularize()} repairs the \strong{time axis}: for each block it inserts a row at
every missing timestamp. Whether a block needs repair is decided by the
robust \code{\link[=check_uniform_sampling_intervals]{check_uniform_sampling_intervals()}} detector, which distinguishes
genuine dropped samples from data that only \emph{looks} irregular -- notably
high-rate trackers that report integer-millisecond timestamps for
sub-millisecond samples (these are left untouched, so genuine samples are
never collapsed). For blocks it does repair, it rebuilds the ideal uniform
grid at the detected sampling period and inserts the missing rows. Inserted
rows carry \code{NA} for the pupil and gaze channels and are flagged in a new
logical \code{is_resampled} column so they can be tracked downstream.

It does \strong{not} fill the inserted values itself; that is the job of
\code{\link[=interpolate]{interpolate()}}. Running \code{regularize()} therefore turns the
"dropped-sample" problem into the ordinary "missing-value" (\code{NA}) problem
that the rest of the pipeline already handles.

For data that is already uniformly sampled (e.g., EyeLink), \code{regularize()}
is a guaranteed no-op: no rows are inserted, no \code{is_resampled} column is
added, and the data is returned unchanged.

If a block's spacing is not \emph{predominantly} regular -- for example, a tracker
that systematically drops every Nth sample, or a recording with a very large
internal gap that should have been split into separate blocks -- rebuilding a
dense grid would fabricate an implausible amount of data. In that case
\code{regularize()} leaves the block untouched rather than synthesizing the grid.
}
\note{
This step is part of the \code{glassbox()} preprocessing pipeline and runs
\strong{automatically by default} (it is a no-op unless irregular sampling is
detected). Opt out with \code{glassbox(regularize = FALSE)}, or tune the guard
with \code{glassbox(regularize = list(max_inflation = ...))}. Advanced users may
call it directly if needed.
}
\seealso{
\code{\link[=interpolate]{interpolate()}} for filling the gaps left by dropped
samples, and \code{\link[=glassbox]{glassbox()}} for the recommended way to run this step
as part of the full \code{eyeris} glassbox preprocessing pipeline.
}
