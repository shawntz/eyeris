% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-resample.R
\name{resample}
\alias{resample}
\title{Resample an irregularly-sampled pupil timeseries onto a uniform grid}
\usage{
resample(eyeris, verbose = TRUE, call_info = NULL)
}
\arguments{
\item{eyeris}{An object of class \code{eyeris} derived from \code{\link[=load_asc]{load_asc()}}}

\item{verbose}{A flag to indicate whether to print detailed logging messages.
Defaults to \code{TRUE}}

\item{call_info}{A list of call information and parameters. If not provided,
it will be generated from the function call}
}
\value{
An \code{eyeris} object whose \code{timeseries} blocks have been placed on a
uniform time grid, with a new logical \code{is_resampled} column marking inserted
(gap) rows.
}
\description{
Places each recording segment onto the expected uniform (consistently-spaced)
sampling grid: local sub-period timing jitter is smoothed by linear
interpolation, and longer-than-expected gaps (dropped samples) become explicit
\code{NA} rows for the later \code{\link[=interpolate]{interpolate()}} step to handle.
}
\details{
Most of the \code{eyeris} pipeline (e.g., \code{\link[=detransient]{detransient()}},
\code{\link[=lpfilt]{lpfilt()}}, \code{\link[=downsample]{downsample()}}) assumes a fixed sampling rate.
EyeLink trackers honor that assumption by zero-filling missing pupil samples,
but some hardware instead \emph{drops} samples entirely when pupil data is
missing, leaving holes in the otherwise evenly-spaced time vector. Those
holes silently distort any rate-dependent step.

\code{resample()} repairs the \strong{time axis} in two stages. Whether a block needs
repair is decided by the robust \code{\link[=check_uniform_sampling_intervals]{check_uniform_sampling_intervals()}} detector,
which distinguishes genuine dropped samples from data that only \emph{looks}
irregular -- notably high-rate trackers that report integer-millisecond
timestamps for sub-millisecond samples (these are left untouched, so genuine
samples are never collapsed). For blocks it does repair:
\enumerate{
\item \strong{Build the target grid.} The uniform grid is anchored on the first
\emph{reliable} regular interval -- the first observed interval that matches the
expected sampling period -- rather than on the first timestamp. This keeps
early sub-period jitter (e.g., intervals of 3, 3, 4, 4 ms at a 4 ms period)
from offsetting the whole grid. The grid is then extended in both
directions at the expected period so that it spans every observed
timestamp, including any samples that precede the anchor (back-extension).
\item \strong{Resample onto the grid.} Observed samples are placed on the grid by
linear interpolation: samples that land on a grid point are kept verbatim,
and short/jittered intervals are interpolated across so their values
contribute to the regular grid. Any observed interval longer than the
expected period is treated as a real gap: the missing grid sample(s) inside
it are inserted as \code{NA} rather than interpolated across, and flagged in a
new logical \code{is_resampled} column so they can be tracked downstream.
}

\code{resample()} does \strong{not} fill the inserted \code{NA} values itself; that is the
job of \code{\link[=interpolate]{interpolate()}}, which decides how much of a missing span to
fill according to its own missing-data policy. Running \code{resample()} therefore
turns the "dropped-sample" problem into the ordinary "missing-value" (\code{NA})
problem that the rest of the pipeline already handles.

For data that is already uniformly sampled (e.g., EyeLink), \code{resample()} is a
guaranteed no-op: no rows are inserted, no \code{is_resampled} column is added, and
the data is returned unchanged. The same is true of a block whose surviving
samples already form a uniform (coarser) grid -- e.g., pure systematic
decimation -- where there is nothing to insert without fabricating data.
}
\note{
This step is part of the \code{glassbox()} preprocessing pipeline and runs
\strong{automatically by default} (it is a no-op unless irregular sampling is
detected). Opt out with \code{glassbox(resample = FALSE)}. Advanced users may call
it directly if needed.
}
\seealso{
\code{\link[=interpolate]{interpolate()}} for filling the gaps left by dropped
samples, and \code{\link[=glassbox]{glassbox()}} for the recommended way to run this step
as part of the full \code{eyeris} glassbox preprocessing pipeline.
}
