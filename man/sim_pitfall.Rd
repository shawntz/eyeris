% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-showcase.R
\name{sim_pitfall}
\alias{sim_pitfall}
\title{Demonstrate a preprocessing-step ordering pitfall (wrong vs right)}
\usage{
sim_pitfall(
  name = c("interp_before_deblink", "lpfilt_before_interp", "lpfilt_before_detransient",
    "naive_downsample", "omit_detrend"),
  seed = 1L,
  params = NULL,
  plot = TRUE,
  preview_window = NULL,
  verbose = TRUE
)
}
\arguments{
\item{name}{Which pitfall to demonstrate. One of \code{"interp_before_deblink"},
\code{"lpfilt_before_interp"}, \code{"lpfilt_before_detransient"},
\code{"naive_downsample"}, or \code{"omit_detrend"}}

\item{seed}{Integer random seed passed to \code{\link[=simulate_eyeris]{simulate_eyeris()}}.
Defaults to \code{1}}

\item{params}{Optional parameter list from \code{\link[=sim_params]{sim_params()}}. When
\code{NULL} (default), scenario-tuned parameters are used so the pitfall is
clearly exposed}

\item{plot}{Logical; draw the wrong-vs-right comparison. Defaults to \code{TRUE}}

\item{preview_window}{Optional length-2 numeric \code{c(start, end)} in seconds. If
\code{NULL} (default), a window bracketing the key feature is chosen
automatically}

\item{verbose}{Logical; print a short summary. Defaults to \code{TRUE}}
}
\value{
An object of class \code{eyeris_showcase} (a list) with elements: \code{name},
\code{seed}, \code{params}, \code{sim} (the simulated object), \code{wrong} (the wrong-order
\code{eyeris} object, or the captured error condition for the crash guardrail),
\code{right} (the right-order \code{eyeris} object), \code{wrong_steps}/\code{right_steps}
(applied step orders), \code{metrics} (a data frame), \code{window}, and \code{extra}.
}
\description{
Generates a synthetic recording tuned to expose a specific ordering pitfall,
runs the pipeline both the wrong way and the right way on the same data, and
returns an \code{eyeris_showcase} object carrying both results and a quantitative
metric that captures the damage the wrong order causes. See
\link{eyeris-showcase} for the catalog of pitfalls.
}
\examples{
# compute the metric without plotting
res <- sim_pitfall("interp_before_deblink", seed = 1, plot = FALSE)
res$metrics

\donttest{
pdf(tempfile(fileext = ".pdf"))
sim_pitfall("lpfilt_before_detransient", seed = 1)
dev.off()
}

}
\seealso{
\code{\link[=sim_step_showcase]{sim_step_showcase()}}, \code{\link[=simulate_eyeris]{simulate_eyeris()}},
\code{\link[=showcase_metric]{showcase_metric()}}
}
