% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-resample.R
\name{resample_block}
\alias{resample_block}
\title{Resample a single block onto a uniform time grid}
\usage{
resample_block(block_df, block_label = NULL, verbose = TRUE)
}
\arguments{
\item{block_df}{A single block's timeseries data frame (must contain a
\code{time_orig} column in milliseconds)}

\item{block_label}{Optional character label used in messages}

\item{verbose}{A flag to indicate whether to print detailed logging messages}
}
\value{
The block data frame placed on a uniform grid, with inserted (gap)
rows marked in a logical \code{is_resampled} column. Returned unchanged (with no
\code{is_resampled} column added) when no reconstruction is needed.
}
\description{
Places one block's samples onto the expected uniform sampling grid:
interpolates values for sub-period timing jitter and inserts \code{NA} rows at
longer-than-expected gaps (dropped samples).
}
\details{
This function is called by the exposed wrapper \code{\link[=resample]{resample()}}.
}
\keyword{internal}
