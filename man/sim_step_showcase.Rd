% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-showcase.R
\name{sim_step_showcase}
\alias{sim_step_showcase}
\title{Demonstrate what a single preprocessing step does to synthetic pupil data}
\usage{
sim_step_showcase(
  step = c("deblink", "detransient", "interpolate", "lpfilt", "downsample", "detrend",
    "zscore"),
  seed = 1L,
  params = sim_params(),
  preview_window = NULL,
  verbose = TRUE
)
}
\arguments{
\item{step}{Which preprocessing step to showcase. One of \code{"deblink"},
\code{"detransient"}, \code{"interpolate"}, \code{"lpfilt"}, \code{"downsample"}, \code{"detrend"},
or \code{"zscore"}}

\item{seed}{Integer random seed passed to \code{\link[=simulate_eyeris]{simulate_eyeris()}}.
Defaults to \code{1}}

\item{params}{A parameter list from \code{\link[=sim_params]{sim_params()}}. Defaults to
\code{sim_params()}}

\item{preview_window}{Optional length-2 numeric \code{c(start, end)} in seconds. If
\code{NULL} (default), a window bracketing the relevant artifact is chosen
automatically}

\item{verbose}{Logical; print status messages. Defaults to \code{TRUE}}
}
\value{
Invisibly, a list with the \code{eyeris} object (\code{obj}), the applied
\code{steps}, and the preview \code{window}.
}
\description{
Generates a synthetic recording with \code{\link[=simulate_eyeris]{simulate_eyeris()}}, runs the
canonical pipeline up to and including the requested \code{step}, and plots the
before/after of each step (via \code{\link[=plot.eyeris]{plot.eyeris()}}) on a preview window
chosen to bracket the artifact that \code{step} targets.
}
\examples{
\donttest{
pdf(tempfile(fileext = ".pdf"))
sim_step_showcase("deblink", seed = 1)
dev.off()
}

}
\seealso{
\code{\link[=sim_pitfall]{sim_pitfall()}} for wrong-vs-right step-ordering
demonstrations.
}
