% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-checks.R
\name{check_uniform_sampling_intervals}
\alias{check_uniform_sampling_intervals}
\title{Check for uniform (consecutive, equal) sampling intervals}
\usage{
check_uniform_sampling_intervals(
  time_vector,
  hz = NULL,
  blocks = NULL,
  tolerance = 0.5,
  block_label = NULL,
  verbose = TRUE
)
}
\arguments{
\item{time_vector}{Numeric vector of sample timestamps (in milliseconds).}

\item{hz}{Optional known sampling rate in Hz (e.g., from the file header).
Used to annotate the warning with the nominal rate and to cross-check for
systematic dropout.}

\item{blocks}{Optional vector (same length as \code{time_vector}) identifying the
recording segment each sample belongs to. When supplied, intervals are
only compared \emph{within} each segment.}

\item{tolerance}{Relative tolerance for the systematic-dropout cross-check
(default \code{0.5}). A rate mismatch is flagged when the data-derived interval
exceeds the nominal interval (\code{1000 / hz}) by more than this fraction.}

\item{block_label}{Optional character label used in the warning to identify
the segment being checked (e.g., \code{"block_1"}).}

\item{verbose}{Logical. Whether to emit the warning message (default \code{TRUE}).}
}
\value{
Invisibly, a list (or, when \code{blocks} is supplied, a list of such
lists, one per segment) summarizing the check with elements: \code{uniform}
(logical), \code{rate_mismatch} (logical; TRUE when the effective rate is lower
than the nominal \code{hz}), \code{expected_interval} (ms), \code{n_intervals},
\code{n_irregular}, \code{n_missing_samples} (estimated number of dropped samples),
and \code{prop_irregular}. A warning is emitted via \code{\link[=log_warn]{log_warn()}} when irregular
intervals or a rate mismatch are detected.
}
\description{
Validates that consecutive samples in a timeseries are uniformly spaced.
Different eye trackers have hardware-specific quirks that violate the
uniform-sampling assumption baked into the \code{eyeris} pipeline. The most
relevant here is that some hardware \emph{drops} samples when pupil data is
missing, rather than zero-filling the gap (as EyeLink does). Dropped samples
leave holes in the otherwise evenly spaced time vector, and because
downstream steps (e.g., \code{\link[=detransient]{detransient()}}, \code{\link[=lpfilt]{lpfilt()}},
\code{\link[=downsample]{downsample()}}) assume a fixed sampling rate, those holes silently
distort the results. This guardrail surfaces the quirk early with an
informative warning so it can be addressed before preprocessing.
}
\details{
The expected inter-sample interval is inferred from the data as the modal
(most frequent) positive interval, which is robust both to a minority of
irregular intervals and to sub-millisecond timestamp rounding at high
sampling rates. Any nonzero interval not exactly equal to this modal
interval is marked irregular; zero-length intervals (the tell-tale of
integer-millisecond rounding of sub-millisecond samples) are exempt.
Intervals longer than the mode are additionally used to estimate the number
of dropped samples.
When the timeseries spans multiple recording segments (\code{blocks}), each
segment is checked independently so that the expected gap \emph{between} segments
is not mistaken for a dropped sample.

Sporadic dropout leaves visible gaps, but \emph{systematic} dropout (e.g., every
Nth sample missing) leaves a uniform but coarser grid that the gap check
alone cannot see. When the nominal sampling rate (\code{hz}) is known, it is
cross-checked against the data-derived interval to surface this case too,
while avoiding false positives from high-rate trackers that report
integer-millisecond timestamps for sub-millisecond samples.
}
\keyword{internal}
