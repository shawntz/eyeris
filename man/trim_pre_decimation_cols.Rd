% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-handler.R
\name{trim_pre_decimation_cols}
\alias{trim_pre_decimation_cols}
\title{Retain only the columns needed to plot pre-decimation pipeline steps}
\usage{
trim_pre_decimation_cols(data)
}
\arguments{
\item{data}{A single block of time series data (a data frame)}
}
\value{
A data frame containing the timing columns (\code{block}, \code{time_orig},
\code{time_secs}, \code{time_scaled}) that are present plus all \verb{pupil_*} columns. A
\code{time_scaled} column mirroring \code{time_secs} is added if it is missing.
}
\description{
Subsets a block of time series data down to the timing columns and pupil
signal columns. This trimmed, full-resolution copy is stashed in
\code{eyeris$timeseries_pre_decimation} before a \code{downsample()}/\code{bin()} step
replaces the working time series with its decimated counterpart, so that
diagnostic plots of earlier steps can be shown at the original sampling
rate rather than the decimated rate (see issue #294).
}
\keyword{internal}
