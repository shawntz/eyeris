% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-eyeris.R
\name{simulate_eyeris}
\alias{simulate_eyeris}
\title{Generate a synthetic \code{eyeris} object with realistic pupil characteristics}
\usage{
simulate_eyeris(seed = 1L, params = sim_params(), block = 1L, verbose = TRUE)
}
\arguments{
\item{seed}{Integer random seed for reproducible generation. Defaults to \code{1}}

\item{params}{A parameter list from \code{\link[=sim_params]{sim_params()}}. Defaults to
\code{sim_params()}}

\item{block}{Numeric block label written into the time series. Defaults to
\code{1}}

\item{verbose}{Logical; print a short status message. Defaults to \code{TRUE}}
}
\value{
An object of S3 class \code{eyeris} (see the \emph{Anatomy of an \code{eyeris}
Object} vignette --- \code{vignette("anatomy", package = "eyeris")}), with
a \code{"sim_truth"} attribute describing the ground-truth signal.
}
\description{
Simulates a pupil time series whose statistical and morphological
characteristics resemble real EyeLink recordings --- a tonic baseline with
slow drift and hippus, canonical task-evoked phasic dilations, blinks with
partial-occlusion flank spikes, isolated transient artifacts, measurement
noise, and optional high-frequency line noise --- and wraps it in a fully
valid S3 \code{eyeris} object.

Because the returned object is byte-compatible with the output of
\code{\link[=load_asc]{load_asc()}}, it flows unchanged through the entire \code{eyeris}
pipeline: \code{\link[=deblink]{deblink()}}, \code{\link[=detransient]{detransient()}},
\code{\link[=interpolate]{interpolate()}}, \code{\link[=lpfilt]{lpfilt()}}, \code{\link[=downsample]{downsample()}},
\code{\link[=detrend]{detrend()}}, \code{\link[=zscore]{zscore()}}, \code{\link[=plot.eyeris]{plot.eyeris()}},
\code{\link[=epoch]{epoch()}}, and \code{\link[=summarize_confounds]{summarize_confounds()}}.
}
\details{
The signal is generated deterministically given \code{seed}: the same \code{seed} and
\code{params} always yield an identical object, and the global random number
generator state is left untouched (generation is confined via
\code{\link[withr:with_seed]{withr::with_seed()}}). See \code{\link[=sim_params]{sim_params()}} for the full generative
model and every tunable component.

The clean latent signal (before artifacts and noise) and the indices of the
injected artifacts are attached to the returned object as the attribute
\code{"sim_truth"} for use as ground truth in quantitative demonstrations; note
that this attribute does \strong{not} survive the pipeline step functions (which
rebuild the time series data frame), so downstream tooling recomputes ground
truth from a retained copy rather than relying on the attribute.
}
\examples{
# generate a synthetic recording and run it through the pipeline
sim <- simulate_eyeris(seed = 1, params = sim_params(duration_secs = 20))

\donttest{
out <- sim |>
  eyeris::deblink() |>
  eyeris::detransient() |>
  eyeris::interpolate() |>
  eyeris::lpfilt() |>
  eyeris::zscore()

pdf(tempfile(fileext = ".pdf"))
plot(out, seed = 1)
dev.off()
}

}
\seealso{
\code{\link[=sim_params]{sim_params()}} to configure the synthetic signal.
}
