% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-render_report.R
\name{make_run_dir_name}
\alias{make_run_dir_name}
\title{Build the canonical run-directory basename}
\usage{
make_run_dir_name(run_num, task = NULL)
}
\arguments{
\item{run_num}{Run number (numeric or character coercible to numeric)}

\item{task}{Optional BIDS task name. Defaults to \code{NULL}}
}
\value{
A character string, e.g. \code{"task-study_run-01"} or \code{"run-01"}
}
\description{
Returns the basename used for both the per-run figure directory and the
filename prefix of every figure it contains. When \code{task} is supplied, the
name is namespaced as \verb{task-\{task\}_run-XX} so that different tasks sharing
the same run number (a valid BIDS pattern, e.g. \code{task-study_run-01} and
\code{task-test_run-01}) do not collide. When \code{task} is \code{NULL}, the legacy
\code{run-XX} form is returned for backwards compatibility.
}
\details{
This is the single source of truth for run-directory naming: every writer
builds the directory and its contained filenames from it, and every reader
either globs with \code{run_dir_pattern()} (then parses the numeric run via
\verb{run-(\\\\d+)}) or derives filenames from \code{basename(run_dir)}.
}
\keyword{internal}
