% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-render_report.R
\name{make_gap_filter_provenance_note}
\alias{make_gap_filter_provenance_note}
\title{Build a data-provenance note when filtering ran over long interpolation gaps}
\usage{
make_gap_filter_provenance_note(eyeris)
}
\arguments{
\item{eyeris}{An \code{eyeris} object}
}
\value{
A markdown string for the report (empty string if no such filtering
over gaps occurred, or if \code{max_gap_ms} is not a single finite numeric value)
}
\description{
If \code{lpfilt()} and/or \code{downsample()} were run on data that still contained
gaps left as \code{NA} by \code{interpolate(max_gap_ms)}, those steps filtered over the
gaps (temporarily filling and then re-masking them), which can slightly bias
the valid samples adjacent to each gap. This records that fact as a
"Data Quality Notes" section in the HTML report so it is part of the
preprocessing provenance.
}
\keyword{internal}
