# Check if baseline mean is zero

Validates that baseline mean is not zero for divisive baseline
correction.

## Usage

``` r
check_baseline_mean(x)
```

## Arguments

- x:

  The baseline mean value to check

## Value

No return value; throws error if baseline mean is zero
