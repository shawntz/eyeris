% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-render_report.R
\name{filter_task_run_dirs}
\alias{filter_task_run_dirs}
\title{Filter run-directory basenames to a single task}
\usage{
filter_task_run_dirs(dirs, task = NULL)
}
\arguments{
\item{dirs}{Character vector of directory basenames}

\item{task}{Optional BIDS task name. Defaults to \code{NULL}}
}
\value{
The subset of \code{dirs} belonging to \code{task}
}
\description{
Given a vector of directory basenames (as returned by \code{list.dirs(..., full.names = FALSE)}), keep only the valid run directories that belong to
\code{task}. When \code{task} is \code{NULL} (or empty), only legacy task-less \code{run-XX}
directories are kept. Comparison of the task component is done by exact
string equality (not regex), so task names containing regex metacharacters
or underscores are handled correctly.
}
\keyword{internal}
