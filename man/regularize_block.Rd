% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-regularize.R
\name{regularize_block}
\alias{regularize_block}
\title{Reconstruct a uniform time grid for a single block}
\usage{
regularize_block(
  block_df,
  max_inflation = 2,
  block_label = NULL,
  verbose = TRUE
)
}
\arguments{
\item{block_df}{A single block's timeseries data frame (must contain a
\code{time_orig} column in milliseconds)}

\item{max_inflation}{The maximum allowed row-count growth factor (see
\code{\link[=regularize]{regularize()}})}

\item{block_label}{Optional character label used in messages}

\item{verbose}{A flag to indicate whether to print detailed logging messages}
}
\value{
The block data frame placed on a uniform grid, with inserted rows
marked in a logical \code{is_resampled} column. Returned unchanged (aside from
adding an all-\code{FALSE} \code{is_resampled} column) when no reconstruction is
needed or when reconstruction would be pathological.
}
\description{
Inserts placeholder rows at the timestamps a tracker dropped, so that the
block's \code{time_orig} is evenly spaced at the inferred sampling period.
}
\details{
This function is called by the exposed wrapper \code{\link[=regularize]{regularize()}}.
}
\keyword{internal}
